function matchHouses(step) {
	return step === 0 ? 0 : (step * 5 ) + 1 ;
} 
//Another Solution

function matchHouses (step) {
    if (step === 0) 
        return 0 ;

    else (step * 5) + 1 ;
}